class Bot extends Player {
  constructor(number) {
    this._number = number
  }

  number() {
    this.number
  }

  // createBots(number) {
  //   const times = x => f => {
  //     for (i = 0, i == number, i++) {
  //       new Bot
  //       times (x - 1) (f)
  //     }
  //   }
  // }
}
